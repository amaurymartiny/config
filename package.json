{
  "author": "Amaury Martiny <amaury.martiny@protonmail.com>",
  "description": "Mostly common-sense configuration files for `eslint` and `tsconfig`",
  "license": "MIT",
  "repository": "https://github.com/amaurymartiny/config",
  "private": true,
  "scripts": {
    "deploy": "lerna version --conventional-commits && lerna publish from-git"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.18.1",
    "typescript": "^3.6.4"
  }
}
